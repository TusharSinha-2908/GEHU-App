import 'package:flutter/material.dart';

class Placement_Screen extends StatelessWidget {
  static const String id = 'Placement Screen';
  const Placement_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
