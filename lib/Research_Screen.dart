import 'package:flutter/material.dart';

class Research_Screen extends StatelessWidget {
  static const String id = 'Research Screen';
  const Research_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
